# File Watcher Environment Variables
# Copy this to your Excel file watcher service directory

# Firebase Configuration (from root .env.local)
VITE_FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY=your-private-key
FIREBASE_CLIENT_EMAIL=your-client-email
VITE_FIREBASE_TENANT_ID=default

# Excel Folder to Watch
# Path to the folder where users save Excel files
EXCEL_WATCH_FOLDER=./excel-imports

# Debounce Time (milliseconds)
# Wait this long after file change before processing
# Increase if your Excel files are large (slow to save)
DEBOUNCE_TIME=2000

# File Lock Timeout (milliseconds)
# How long to wait for Excel to release the file
FILE_LOCK_TIMEOUT=5000
